<h1 class="d-flex justify-content-center my-4">List of Socks</h1>

<div class="row d-flex justify-content-center" >
  <% @socks.each do |sock| %>
    <div class="card-product my-3 mx-5">
    <%= link_to sock_path(sock) do %>
      <%= cl_image_tag(sock.photo.key, width: 200, height: 200, crop: :fill) %>
    <% end %>
      <div class="card-product-infos">
        <h2><%= sock.price %> €</h2>
        <p><%= sock.description.first(50) %>...</p>
      </div>
    </div>
  <% end %>
</div>
